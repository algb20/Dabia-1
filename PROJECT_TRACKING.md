# DABIA — سجل التتبع الرئيسي (يُقرأ ويُحدَّث في بداية كل جلسة عمل)

## ⚠️ قاعدة إلزامية لأي عمل على هذا المشروع
قبل أي تعديل جديد، اقرأ هذا الملف كاملاً أولاً. بعد أي تعديل، حدّث حالة
البند فوراً (لا تؤجل التحديث). لا تُغلق أي بند كـ "✅ مكتمل" إلا بعد فحص
الكود الفعلي (ليس افتراضاً) وتأكيد أن السبب الجذري عولج لا الأعراض فقط.

## قواعد ثابتة للعمل (لا تُخرَق)
1. أي عطل RLS يُفحص أولاً بـ: هل هو جدول `public.X` أم `storage.objects`؟
   هذان نوعان مختلفان من السياسات ولا يجب افتراض أحدهما بدون تحقق.
2. أي `useState`/`useUserAuth`/hook مكلف يُستدعى مرة واحدة في المكوّن الأب
   (Tab/Page) ويُمرَّر كـ prop للأبناء (Card/Item) — أبداً داخل عنصر يتكرر
   في قائمة (list/map)، لأن هذا يُبطئ أو يُعطّل التطبيق بالكامل.
3. عند إضافة دالة جديدة بنفس الاسم المحتمل لدالة قديمة، يُبحث أولاً بـ
   `grep -c "function NAME"` في كل الملف قبل الإضافة لمنع التكرار.
4. كل ميزة "جديدة" يجب أن تتضمن من أول مرة: (أ) الواجهة (ب) الاتصال
   الحقيقي بـ Supabase (ج) SQL/RLS اللازم (د) اختبار توازن الأقواس —
   وليس فقط الواجهة البصرية.
5. بعد كل دفعة تعديلات، يُشغَّل فحص توازن الأقواس بطريقة استبعاد
   template literals والتعليقات معاً (وليس فقط تعليقات // البسيطة).

---

## 📋 قائمة بنود الجلسة الكبيرة (المرسلة في الرسالة الطوينة) — حالة كل بند

| # | البند | الحالة | ملاحظة |
|---|---|---|---|
| 1 | الترجمة تعمل لكل صفحة منفصلة | 🟡 أُعيد بناؤها v40 | يحتاج تأكيد اختبار فعلي بعد v40 |
| 2 | حفظ بطاقة الهوية لا يعمل (ذاكرة حقيقية) | 🟡 أُصلح RLS جزئياً v40 | يحتاج تأكيد اختبار فعلي |
| 3 | رفع صورة البروفايل يفشل (RLS) | ✅ أُصلح v40 (Storage RLS) | السبب الجذري الحقيقي كان غير معالَج بالكامل قبل v40 |
| 4 | زر ليل/نهار: أيقونة فقط بجانب الترجمة | ✅ أُصلح v40 | |
| 5 | المنتجات المحفوظة لا تظهر + لا يوجد أيقونة حفظ | ✅ أُصلح v40 | نظام `saved_products` حقيقي + زر Bookmark |
| 6 | منتجاتي: المنتج المضاف لا يظهر في متجري | ✅ RLS أُصلح v40 | كان بسبب auth_id فارغ يمنع الإدراج بصمت |
| 7 | تخفيض السعر لا يظهر/لا يعمل | ✅ أُصلح v38 (original_price) | يحتاج تأكيد الربط الكامل بالواجهة |
| 8 | متابعة الطلبات/المشتريات وهمية | ✅ نظام كامل بُني v38 | `/orders`, `/orders/seller`, ORDER_STATUS_FLOW |
| 9 | زر "الأمان" في البروفايل لا يعمل | 🔴 لم يُفحص بعد | **التالي مباشرة** |
| 10 | نظام تقييم ومتابعة التجار لمنع التحايل | 🔴 لم يُبنَ | يحتاج جدول reviews + منطق "تقييم بعد تسليم فعلي فقط" |
| 11 | الصفحة الاجتماعية: التعليق لا يُحفظ | 🔴 لم يُفحص | يحتاج فحص RLS جدول product_comments/posts |
| 12 | حفظ منشور/إعلان في الصفحة الاجتماعية | 🔴 لم يُبنَ | يحتاج زر + جدول saved_posts |
| 13 | مشاركة حقيقية بكل التطبيقات + رابط دقيق للمنشور | 🟡 جزئي v37 | بُني لمنتجات فقط (ShareModal)، ينقصه نفس الشيء للمنشورات (posts) |
| 14 | زر "إعادة النشر" (Repost) | 🔴 لم يُبنَ | |
| 15 | التصويت بالنجوم في "الصفحة الرسمية" وهمي | 🔴 لم يُفحص/يُصلح | يحتاج تحديد أي صفحة بالضبط (تقييم منتج؟ صفحة عامة؟) |
| 16 | مشاركة منتج من صفحة العرض إلى الاجتماعي | 🟡 جزئي | زر مشاركة موجود، لكن "نشر فعلي كمنشور بالتاب الاجتماعي" (sharePostFromProduct) موجود بالدوال لكن لم يُربط بزر في الواجهة الرئيسية بعد |
| 17 | وصف المنتج لا يظهر | 🔴 لم يُفحص | |
| 18 | لا يوجد محرك بحث حقيقي في صفحة المنتج | 🔴 لم يُبنَ | SearchOverlay موجود لكن قد لا يكون مربوطاً بزر واضح داخل تاب معيّن |
| 19 | صفحة Discover تحتاج تطوير حقيقي كامل | 🔴 لم يُبنَ بعد | ما زالت تعتمد جزئياً على AIInsight الوهمية |
| 20 | صفحة Space: إعلان عن حدث/مزاد/بث قادم | 🔴 لم يُبنَ | يحتاج جدول scheduled_events |
| 21 | نظام بث (Live) متطور وآمن مع بيع/طلب مسبق | 🔴 لم يُبنَ (يحتاج بحثاً تقنياً) | بث فيديو حقيقي يحتاج خدمة خارجية (خارج Supabase) |
| 22 | نظام إنشاء مجموعات (Groups) | 🔴 لم يُبنَ | مختلف عن "Group Deals" الموجودة — مجموعات نقاش/مجتمع |
| 23 | تجار/مستخدمون حقيقيون لا يظهرون رغم دخول الزوار | 🟡 مرتبط بالبند 6 | يجب أن ينحل تلقائياً بعد إصلاح RLS للمنتجات |
| 24 | السماح بإنشاء حسابات متعددة لنفس المستخدم | 🔴 قرار متعارض | ⚠️ يتعارض مع بند "منع الحسابات المكررة" الذي بنيناه سابقاً بناءً على طلب سابق — **يحتاج توضيحاً صريحاً من المستخدم أولاً** |
| 25 | نظام بحث/استرجاع حساب فعال | ✅ بُني v38 | `/find-account` — يحتاج تأكيد اختبار |
| 26 | "فضاء تجاري" لم يظهر في التطبيق | 🔴 غامض - يحتاج توضيح | هل يقصد BusinessTab الموجود، أم شيء مختلف؟ |
| 27 | نظام تلقائي مستمر يجلب منتجات حقيقية من Pi Network blockchain | 🔴 لم يُبنَ (ضخم) | لا يوجد API عام موثّق لمسح منتجات من "بلوكشين Pi" — يحتاج بحثاً تقنياً جاداً أولاً قبل أي وعد |

## قرار صريح مطلوب من المستخدم (تعارض حقيقي)
البند 24 (حسابات متعددة) يتعارض تقنياً مع نظام "منع الحسابات المكررة
بمعرّف Pi" المبني حالياً. يجب توضيح: هل المقصود عدة حسابات بأدوار مختلفة
(مثلاً حساب شرائي + حساب تجاري) لنفس الشخص، أم فتح حسابات غير محدودة
بنفس Pi ID؟ هذا يُغيّر التصميم الأمني للمشروع بالكامل.

## البند 27 يحتاج توضيحاً تقنياً قبل البدء
"بلوكشين Pi Network" لا يحتوي "منتجات" بالمعنى الذي تصفه (هو سجل معاملات
مالية بحت، وليس سوقاً إلكترونياً). المقصود الأرجح فعلياً: نظام يزحف
(crawl) على مواقع Pi Apps الأخرى المعروفة (App Studio directory) ويستورد
عروضها. هذا ممكن تقنياً لكنه يحتاج قائمة مصادر محددة، وليس "قراءة من
البلوكشين" حرفياً. يُطرح على المستخدم للتوضيح قبل البناء.
